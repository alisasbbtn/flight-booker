Thank you for booking your tickets!
<%= '=' * 25 %>
Dear <%= @passenger.name %>,
  Your tickets have been booked succesfully.
    FROM: <%= "#{@flight.from_airport.code} #{@flight.from_airport.location}" %>
    TO: <%= "#{@flight.to_airport.code} #{@flight.to_airport.location}" %>
    Date and time: <%= @flight.date_time.strftime("%b %d %Y %H:%M") %>
    Duration: <%= minutes_in_words(@flight.duration) %>

Have a nice flight!
